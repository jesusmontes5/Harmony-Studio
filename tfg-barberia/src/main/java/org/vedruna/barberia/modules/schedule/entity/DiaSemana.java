package org.vedruna.barberia.modules.schedule.entity;

/**
 * Enum de dias de semana en el mismo formato de la base de datos.
 */
public enum DiaSemana {
    /** Lunes. */
    LUNES,
    /** Martes. */
    MARTES,
    /** Miercoles. */
    MIERCOLES,
    /** Jueves. */
    JUEVES,
    /** Viernes. */
    VIERNES,
    /** Sabado. */
    SABADO,
    /** Domingo. */
    DOMINGO
}
