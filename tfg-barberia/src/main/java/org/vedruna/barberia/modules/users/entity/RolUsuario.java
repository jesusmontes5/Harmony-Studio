package org.vedruna.barberia.modules.users.entity;

/**
 * Enum que representa los roles funcionales de la aplicacion.
 */
public enum RolUsuario {
    /** Rol para clientes finales. */
    CLIENTE,
    /** Rol para barberos. */
    BARBERO,
    /** Rol para administracion completa. */
    ADMIN
}
